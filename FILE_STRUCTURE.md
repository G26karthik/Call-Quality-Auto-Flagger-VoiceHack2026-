# Caller.ai ML Pipeline - File Structure

## Current Submission File

**Location:** `submission_ml_v5.csv` (ROOT FOLDER)
- This is the CURRENT and ONLY submission file
- 18 tickets flagged out of 159 test calls
- Generated by train_ml_pipeline_v5.py
- Clean version with NO data leakage

## ML Pipeline Versions (ml_pipeline/ folder)

### Training Scripts (Keep All for History)
1. **train_ml_pipeline.py** - v1 baseline
2. **train_ml_pipeline_v2.py** - Enhanced features
3. **train_ml_pipeline_v3.py** - Reduced features
4. **train_ml_pipeline_v4.py** - Anti-overfitting (5-fold CV)
5. **train_ml_pipeline_v5.py** - CURRENT BEST
   - 10-fold CV
   - Full data training (833 rows)
   - Interaction features
   - Optuna ensemble optimization
   - **NO data leakage** (validation_notes removed)
   - CV F1: 0.9185 ± 0.0731

### Submission Files in ml_pipeline/ (Archive)
- submission_ml.csv (v1)
- submission_ml_v2.csv (v2)
- submission_ml_v3.csv (v3)
- submission_ml_v4.csv (v4)

Note: v5 submission is ONLY in root folder (no duplicate)

## Cleanup Done

### Deleted Files (Redundant)
- ❌ train_ml_pipeline_v4b.py (v4 was better)
- ❌ feature_importance.png, v2, v3 (not needed)
- ❌ precision_recall_curve.png (analysis artifact)
- ❌ evaluation_report.csv, .py (analysis scripts)
- ❌ analyze_patterns.py (analysis script)
- ❌ ml_pipeline/submission_ml_v5.csv (duplicate)

## Answer to Your Questions

### Q1: Which submission file is current?
**A:** `submission_ml_v5.csv` in the **ROOT folder** only
- There is NO duplicate anymore
- This is the file to submit

### Q2: v4 vs v4b?
**A:** Kept v4, deleted v4b
- v4 has better anti-overfitting approach
- v4b was experimental, not needed

### Q3: Why multiple feature_importance.png?
**A:** Deleted all of them
- They were generated during experimentation
- Not needed for final submission
- Can regenerate if needed by running the training scripts

### Q4: evaluation_report and analyze_patterns?
**A:** Deleted
- These were analysis/debugging scripts
- Not needed for competition
- Clean codebase now

## Next Steps

To regenerate v5 submission:
```bash
cd ml_pipeline
python train_ml_pipeline_v5.py
```

This will:
- Train on full 833 labeled samples
- Use 10-fold CV for validation
- Generate submission_ml_v5.csv in ml_pipeline/
- You can then copy to root if needed
